import { StyleSheet } from "react-native";

export const S = StyleSheet.create({
  button: {
    backgroundColor: '#800080',
    padding: 20,
    borderRadius: 5,
    width: 200,
    alignItems: 'center'
  },
  label: {
    color: '#eee',
    fontWeight: 'bold',
  }
})